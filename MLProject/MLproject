name: titanic-survivor-prediction

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      config: {type: str, default: "../.github/workflows/mlflow_train.yaml"}
    command: "python modelling.py --config {config}"
