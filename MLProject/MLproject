name: titanic-survivor-prediction

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "titanic_preprocessing.csv"}
    command: "python mlflow_train.py --data_path {data_path}"
